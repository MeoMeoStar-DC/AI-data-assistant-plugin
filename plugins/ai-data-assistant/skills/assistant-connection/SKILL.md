---
name: assistant-connection
description: 处理已安装 AI 数据助手插件的首次连接、OAuth 重新授权、工具不可见和连接验证。用户提到登录失败、complete 后仍不可用、身份验证按钮消失时使用；正常问数不触发重复连接检查，不负责修改 Auth0 或安装其他插件。
---

# AI 数据助手连接排障

目标是恢复现有插件入口，并以一次 DAU 正式定义读取确认可用。本 Skill 随插件本地安装，排障起步不依赖远程 MCP 工具；不要先要求调用尚不可用的远程工具来认证。

## 定位现有入口

- 核对桌面版实际后端与终端 CLI 的版本、已安装插件来源、启用状态，以及是否存在同名用户级 MCP。只读取必要配置字段，不读取凭据文件，不输出完整配置。
- 当前团队在 Windows CLI `0.144.4` 上复现过错误回调端口和 resource；不要用该版本登录。桌面版内置 `0.154.0-alpha.6.2` 曾通过隔离登录验证，这不代表所有更高版本均已验证。优先使用实际匹配桌面版的后端，不猜路径，不自动升级。
- 插件来源应为 `ai-data-assistant@ai-data-assistant-team`，远程地址为 `https://mcp.aidamms.com/mcp`。发现同名混合配置时先识别实际入口，不删除未知配置，不再次添加 MCP。`codex mcp list` 为空不能单独证明插件未加载。

## 区分状态并恢复

- OAuth 未完成、登录入口消失，或网页显示 `complete` 但仍不可用：通过宿主实际提供的 OAuth 功能为已有入口重新发起登录并打开浏览器。用户已请求恢复连接时可直接执行；只有缓存阻止重新授权时，才退出该入口的 OAuth 后再登录。不得触碰其他连接。
- 需要本人点击界面时，路径为：设置 → 插件 → AI 数据助手 → 打开 MCP 设置 → 找到 `ai-data-assistant` 服务器 → 进行身份验证。账号选择、注册和登录由用户本人完成，保持当前回调进程运行。
- 宿主没有可用登录能力时，提供上述界面步骤；按钮也不可用时，报告实际后端版本与脱敏错误，不虚构登录工具或命令。
- 已认证但出现 `403` / `insufficient_scope`：让管理员核对实际账号角色及客户端允许的权限。角色更新后才重新授权；不自行扩大 scope 或修改 Auth0。缺少 raw 工具不能单独判定登录失败，普通成员通常没有 `metadata.raw.read`。
- 已认证且工具可用，但旧会话找不到工具：先新开会话加载插件，不反复退出登录。
- 一次重新授权后仍失败，停止同条件重试，报告脱敏错误、版本与入口来源。`too_many_entities` 应立即停止重试，不动态注册更多客户端。

## 最小验证与结果

仅在工具可用后调用一次 `get_metric_reference`，查询 `dau`，使用 `detail=summary`、`limit=1`。不调用 `prepare_analysis`，不执行 Tableau、Redshift 业务查询或 SQL，不调用 raw 工具。

只有调用成功，正式指标 ID 为 `dau` 且来源为 `formal_metric_dictionary` 时，报告正式参考连接可用；这不证明仓库查询或 governance 权限已验收。按钮消失、网页 complete、插件启用均不能替代真实调用。

只汇报入口、实际版本、认证状态、验证结果和下一步。不得读取或输出密码、token、完整授权 URL；使用客户端原生浏览器流程，不手工改写 resource、回调或 PKCE 参数。不新增、删除或重装插件/MCP，不修改 Auth0。
